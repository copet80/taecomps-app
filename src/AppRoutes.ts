enum AppRoutes {
  Auth = '/auth',
  Bracket = '/bracket',
  Dashboard = '/',
  Entries = '/entries',
  Login = '/',
  Profile = '/profile',
  Register = '/register',
  ResetPassword = '/resetpassword',
  TournamentDetails = '/tournament',
}

export default AppRoutes;
